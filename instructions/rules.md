# NOS Design Rules

This file is retained as a compatibility pointer only.

The canonical design-principle content now lives in
`content/instructions/base.ts` as `designPrinciplesMarkdown`.

The workbench reference page at `/workbench/instructions/rules` renders from
that canonical fragment. Use the instruction generator on `/workbench` for
tailored project-specific instructions.
